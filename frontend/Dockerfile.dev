# Development Dockerfile for Santa Frontend
# Uses volume mounts for hot-reload with Vite

FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Source code will be mounted as volume
# COPY . .

EXPOSE 5173

# Run Vite dev server with HMR
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
